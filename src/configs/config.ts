export const config = {
  // Add user IDs of admins here
  admins: [
    "U0123456789",
  ],
  // Default messages
  messages: {
    cooldown: "You are doing that too fast. Please wait a moment before trying again.",
    adminOnly: "You do not have permission to execute this command.",
    error: "An unexpected error occurred while processing your request.",
  },
  // Default Block Kit Colors
  colors: {
    primary: "#000000",
    error: "#FF0000",
    success: "#00FF00",
  },
};
